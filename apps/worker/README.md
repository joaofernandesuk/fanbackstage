# FanBackstage Worker

The Phase 0 Celery worker code lives with the API application because it shares the modular-monolith domain layer. This directory reserves the independently deployable worker application boundary.

