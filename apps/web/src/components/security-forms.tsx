"use client";
import { FormEvent, useState } from "react";
import { useSearchParams } from "next/navigation";
import { api, ApiError } from "../lib/api";

export function ForgotPasswordForm() { const [message, setMessage] = useState(""); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const form = new FormData(e.currentTarget); try { const response = await api<{message: string}>("/auth/forgot-password", {method:"POST", body: JSON.stringify({email: form.get("email")})}); setMessage(response.message); } catch (e) { setMessage(e instanceof ApiError ? e.message : "Unable to continue"); } } return <section className="card"><h1>Reset your password</h1><form onSubmit={submit}><label>Email<input required name="email" type="email" /></label><button>Send reset link</button></form>{message && <p role="status">{message}</p>}</section>; }
export function TokenForm({ kind }: {kind: "verify-email" | "reset-password"}) { const params = useSearchParams(); const [message, setMessage] = useState(""); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const form = new FormData(e.currentTarget); const token = String(form.get("token") || params.get("token") || ""); try { const response = await api<{message:string}>(`/auth/${kind}`, {method:"POST", body: JSON.stringify(kind === "reset-password" ? {token, new_password: form.get("password")} : {token})}); setMessage(response.message); } catch(e) { setMessage(e instanceof ApiError ? e.message : "Unable to continue"); } } return <section className="card"><h1>{kind === "verify-email" ? "Verify your email" : "Choose a new password"}</h1><form onSubmit={submit}><label>Token<input required name="token" defaultValue={params.get("token") ?? ""} /></label>{kind === "reset-password" && <label>New password<input required minLength={12} name="password" type="password" /></label>}<button>{kind === "verify-email" ? "Verify email" : "Reset password"}</button></form>{message && <p role="status">{message}</p>}</section>; }
