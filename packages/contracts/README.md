# FanBackstage Contracts

FastAPI OpenAPI is the Phase 0 contract source of truth. Generated TypeScript artifacts, if introduced, must be reproducible and committed separately from handwritten application logic.

